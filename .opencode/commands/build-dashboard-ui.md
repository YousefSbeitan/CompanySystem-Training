---
description: Generate professional Dashboard, Layout, Navigation and Branding UI for CompanySystem. Creates application shell with permission-based menus using existing Auth UI and CRUD pages.
agent: ui-builder
---

Load the build-dashboard-ui skill and execute it.

Create the CompanySystem professional dashboard experience using the existing project.

The backend is already completed.

Authentication UI is already generated by:

build-auth-ui

CRUD pages are already generated by:

build-ui


Read:

- CompanySystem.Web/Views/Shared/_Layout.cshtml
- CompanySystem.Web/Views/Auth/
- CompanySystem.Web/wwwroot/js/auth.js
- CompanySystem.Web/Controllers/


Detect existing modules from:

CompanySystem.Web/Controllers/


Create or update:

- CompanySystem.Web/Views/Home/Dashboard.cshtml
- CompanySystem.Web/Views/Home/Index.cshtml


Create or update:

- CompanySystem.Web/wwwroot/css/dashboard.css
- CompanySystem.Web/wwwroot/css/theme.css


Create or update:

- CompanySystem.Web/wwwroot/js/dashboard.js


Update carefully:

- CompanySystem.Web/Views/Shared/_Layout.cshtml


Before editing _Layout.cshtml:

ALWAYS preserve:

```razor
@RenderBody()

@RenderSection(...)
```

Keep existing:

- Bootstrap references
- jQuery references
- Existing scripts
- Existing styles


Tasks:

1. Build Application Layout

Create:

- Professional navbar
- Sidebar navigation
- User menu
- Role badge
- Logout button
- Company branding


2. Build Dashboard Page

Create:

Dashboard containing:

- Welcome section
- Current username
- Current role
- Module cards
- Quick navigation


3. Generate Navigation

Inspect controllers:

CompanySystem.Web/Controllers/


Generate menu links only for existing modules.

Examples:

UserController:

Create link:

/User


DepartmentController:

Create link:

/Department


RoleController:

Create link:

/Role


Never invent modules.


4. Build Permission Based Navigation

Use data provided by build-auth-ui:

```javascript
window.currentUser

window.currentUserPermissions
```

Fallback:

```javascript
window.currentUserRole
```


Rules:

- Users menu requires "Users.View" permission
- Departments menu requires "Departments.View" permission
- Roles menu requires "Roles.View" permission
- Notes menu requires "Notes.View" permission
- MainPageSections menu requires "MainPageSections.View" permission
- Permissions menu requires "Permissions.View" permission

Restricted menus hidden by default.

Never show links for modules the user cannot access.


Never change backend authorization.


5. Build Dashboard Statistics

Use existing endpoints only.

Only load statistics for modules the user has permission to view.

Examples:

Users (requires Users.View):

GET:

/User/GetAll

Departments (requires Departments.View):

GET:

/Department/GetAll

Roles (requires Roles.View):

GET:

/Role/GetAll


Calculate counts from returned data.

Never load data for modules the user cannot access.


Support:

Paginated response:

```json
{
 "items": []
}
```


Support:

```json
{
 "data": []
}
```


Support normal arrays.


Never create:

- New APIs
- New controller actions
- Statistics endpoints


6. Authentication Integration

Verify:

auth.js exists.


Use:

```javascript
window.currentUser
window.currentUserRole
```


Do NOT modify:

- Login
- Register
- Logout logic
- Token storage


Those belong to:

build-auth-ui


7. CRUD Integration

Connect dashboard links with pages created by:

build-ui


Do NOT modify:

Views/{EntityName}/


Only link to them.


Security Rules:

DO NOT modify:

- Controllers
- AuthController
- Authorization attributes
- Services
- Interfaces
- DTOs
- Entities
- Repositories
- DbContext
- Migrations
- Program.cs
- appsettings.json


NEVER remove:

```csharp
[Authorize]

[Authorize(Roles="...")]

[RequirePermission("...")]
```


NEVER add or reduce permissions.


Backend security is the source of truth.


Final verification:

Check:

✓ Login works

✓ Logout works

✓ Dashboard opens

✓ Navigation works

✓ Permission menus work

✓ Fallback role menus work

✓ CRUD links work

✓ Layout responsive

✓ No backend files changed


Run:

dotnet build


Final result must be:

0 errors