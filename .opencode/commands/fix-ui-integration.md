---
description: Fix integration issues between Authentication UI, Dashboard UI and CRUD pages for CompanySystem MVC.
agent: ui-builder
---

Load the fix-ui-integration skill and execute it.

Fix the complete UI integration for CompanySystem.

The project already contains:

- Authentication UI
- Dashboard UI
- CRUD Views

Do NOT rebuild existing UI.

Only connect and fix the existing implementation.

---

# Read Existing Files First

Analyze:

## Authentication:

- CompanySystem.Web/Controllers/AuthController.cs

- CompanySystem.Web/Views/Auth/Login.cshtml

- CompanySystem.Web/Views/Auth/Register.cshtml

- CompanySystem.Web/wwwroot/js/auth.js


## Dashboard:

- CompanySystem.Web/Views/Home/

- CompanySystem.Web/Views/Shared/_Layout.cshtml

- CompanySystem.Web/wwwroot/js/dashboard.js

- CompanySystem.Web/wwwroot/css/dashboard.css


## CRUD:

Inspect:

- CompanySystem.Web/Controllers/

- CompanySystem.Web/Views/


---

# Required Fixes


## 1. Fix MVC Authentication Flow

Current problem:

JWT stored in localStorage works with AJAX.

But MVC navigation:

Example:

/Department

returns:

401 Unauthorized

because browser navigation does not send Authorization header.

Fix authentication so:

- MVC pages work
- AJAX calls work
- Existing authorization rules remain


NEVER remove:

```csharp
[Authorize]
```

NEVER remove:

```csharp
[Authorize(Roles="...")]
```

---

## 2. Fix Login Experience

Login page must be standalone.

Remove from Login:

- Sidebar
- Dashboard navbar
- Management menu
- Footer dashboard style


Create professional ASAL Technologies login screen.

Include:

- Company branding
- Clean UI
- Responsive design


---

## 3. Fix Register Access

Register is not public signup.

Remove:

Login page Register link.


Register should be accessible only:

After Admin login.


Admin flow:

Dashboard

↓

Users

↓

Create/Register User


---

## 4. Password Eye Toggle

Add custom password visibility toggle.

Requirements:

- Always visible
- Browser independent
- Shows immediately
- Works while typing


Apply to:

- Login password
- Register password fields


---

## 5. Fix Redirect Flow

After successful login:

Redirect:

FROM:

/

TO:

/Home/Dashboard


After logout:

Redirect:

/Auth/Login


---

## 6. Fix Dashboard Sidebar Links

Fix all navigation links.

Required:

Clicking sidebar items must open:

- Users
- Departments
- Roles
- Notes
- MainPageSections


Do not hardcode missing modules.

Read existing controllers.


---

## 7. Fix Role Based Menu

Use:

```javascript
window.currentUserRole
```

Rules:

Admin:

Show all allowed modules.

Manager/User:

Show only permitted modules.


Frontend hiding is only UI.

Backend authorization remains source of truth.


---

## 8. Preserve Existing Work

Do NOT overwrite:

CRUD pages generated by build-ui.

Do NOT recreate:

Login/Register from zero.

Only improve existing files.


---

# Allowed Modification Scope

Allowed:

```text
Views/Auth/

Views/Home/

Views/Shared/_Layout.cshtml

wwwroot/js/auth.js

wwwroot/js/dashboard.js

wwwroot/css/dashboard.css
```


Only if required:

```text
Controllers/AuthController.cs
```


Forbidden:

```text
Business/

Data/

Shared/
```


---

# Final Verification

Before finishing check:

- Login works

- JWT still saved

- MVC navigation works

- Department opens

- User opens

- Role opens

- Notes opens

- MainPageSection opens

- Dashboard role filtering works

- Logout works

- No security removed


Run:

```bash
dotnet build
```

Required:

0 errors


---

# Final Response

Return summary:

- Fixed files
- Integration changes
- Build result
