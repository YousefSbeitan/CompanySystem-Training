---
description: Generate MVC Razor Views for an entity. Usage: build-ui <EntityName>
agent: ui-builder
---

Load the build-ui skill, then execute it for entity: $ARGUMENTS

The entity name is: $1

Create the folder `CompanySystem.Web/Views/$1/` if it doesn't exist.

Follow the skill instructions exactly. Generate all standard views. Do not modify any files outside the Views/$1/ folder.
